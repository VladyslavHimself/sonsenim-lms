{
  "name": "sonsenim",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "ui:dev": "pnpm --filter ui dev",
    "ui:coverage": "pnpm --filter ui coverage",
    "ui:stage:deploy": "pnpm --filter ui cf:stage:deploy",
    "ui:build": "pnpm --filter ui n:build",

    "api:dev": "pnpm --filter api start",

    "db:migrate": "dbmate migrate",
    "db:status": "dbmate status",

    "api:prod:deploy": "pnpm --filter api cf:prod:deploy",
    "api:staging:deploy": "pnpm --filter api cf:stage:deploy",
    "w:api:dev": "pnpm --filter api w:dev",
    "dev": "pnpm run start:ui & pnpm run start:api",
    "build": "pnpm recursive run build",

    "app:dev": "pnpm -r --parallel --stream --filter ui --filter api run dev",
    "app:staging": "pnpm -r --parallel --stream --filter ui --filter api run staging"
  },
  "keywords": [],
  "author": "Vladyslav Lutchyn <vladyslav.lutchyn@gmail.com>",
  "license": "ISC",
  "packageManager": "pnpm@10.29.1",
  "devDependencies": {
    "wrangler": "^4.63.0"
  }
}
