DATABASE_URL="" // DBMATE db path ( ?sslmode=disable )

// Signing secret for auth tokens. Required — the API refuses to start without it.
// Generate one with: openssl rand -base64 48
// In deployed Workers this comes from `wrangler secret put JWT_SECRET --env <environment>`,
// not from this file.
JWT_SECRET=""
